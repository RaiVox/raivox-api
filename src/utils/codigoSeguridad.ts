// src/utils/codigoSeguridad.ts
export const generarCodigoSeguridad = (): string => {
    // Lógica para generar un código de seguridad (puedes personalizar según tus necesidades)
    const caracteres = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789';
    let codigo = '';
    //for (let i = 0; i < 6; i++) {
    //  codigo += caracteres.charAt(Math.floor(Math.random() * caracteres.length));
    //}
    return codigo;
  };
  